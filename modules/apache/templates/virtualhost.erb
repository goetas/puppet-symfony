<VirtualHost <%= @ip %>:<%= @port %>>
        
    ServerName <%= @server_name %>
    DocumentRoot <%= @docroot %>

    <Location "<%= @docroot %>">
        AllowOverride All
        Require all granted
    </Location>

</VirtualHost>
